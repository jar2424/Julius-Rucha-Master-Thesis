#
# Python dependencies for the thesis computational bundle (07_Thesis_GitHub).
#
# Tested with Python 3.12.
#
# Install:
#   pip install -r requirements.txt
#
# Notes:
# - The analysis notebooks (03_analysis) run without Gurobi.
# - The solver notebooks (02_solver) can use Gurobi (recommended) via PuLP, but will
#   fall back to PuLP's bundled CBC if no Gurobi licence is available.

# Core data processing
numpy>=2.1
pandas>=2.2
openpyxl>=3.1        # required by pandas for .xlsx read/write
xlrd>=2.0            # required by pandas for legacy .xls read

# Notebook runtime (recommended)
jupyterlab>=4.2
ipykernel>=6.29

# Optimisation
pulp>=3.3            # LP/MILP modelling layer (wraps Gurobi or CBC)
gurobipy>=13.0       # requires a valid Gurobi licence

# Visualisation
matplotlib>=3.10
